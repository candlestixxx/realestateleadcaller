# TODO: Immediate Short-Term Tasks

- [ ] Add robust error handling (e.g., try/catch blocks with toast notifications) to all frontend `fetch` calls.
- [ ] Implement a webhook receiving endpoint (`/api/webhooks/crm`) to parse incoming leads from Zillow/FollowUpBoss.
- [ ] Connect the "Direct Mail Tasks" UI button directly to the POST route to allow manual triggering of mail tasks.
- [ ] Add a visual indicator on the Lead Profile page to show which step of the Workflow the lead is currently on.
- [ ] Set up a basic GitHub Actions workflow to run ESLint and Next.js builds on push.

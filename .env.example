# NextAuth
NEXTAUTH_SECRET=your_super_secret_key_here
NEXTAUTH_URL=http://localhost:3000

# Database
DATABASE_URL="file:./dev.db"

# Note: Integration keys (Twilio, Vapi, SendGrid) are managed through the UI settings dashboard,
# not through this .env file, to allow for multi-tenant support.
